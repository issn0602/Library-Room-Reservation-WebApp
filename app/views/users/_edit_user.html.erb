<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="row" style="background-color:transparent;">
      <div class="col-md-6 col-md-offset-3" style="margin-left:40%;margin-right:40%;float:left;width:20%;background-color:transparent;">
        <br><center><h1>Edit Profile</h1></center><br>
          <div class="field">
            <%= f.label :name %><br>
            <%= f.text_field :name, :required => true %>
          </div>
          <div class="field">
            <%= f.label :email %><br>
            <%= f.text_field :email, :required => true %>
          </div>
          <div class="field">
            <%= f.label :password %><br>
            <%= f.password_field :password, :required => true %>
          </div>
          <div class="field">
            <%= f.label :password_confirmation %><br>
            <%= f.password_field :password_confirmation, :required => true %>
          </div>
          <div class="actions">
            <%= f.submit %>
          </div>
  <% end %>

<br><br>

<%= link_to 'Back', users_path %>
</div>
</div>